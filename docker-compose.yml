version: '3.8'

services:
  battlecity:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: battlecity
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - battle-city-network

networks:
  battle-city-network:
    driver: bridge
